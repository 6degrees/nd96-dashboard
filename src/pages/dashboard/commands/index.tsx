import { ScreenCommandPanel } from '@/features/command'

/*
|--------------------------------------------------------------------------
| Screen Commands Page
|--------------------------------------------------------------------------
|
| Renders the remote screen commands page.
|
| Responsibilities:
| - Displays available remote screens.
| - Provides controls for each screen.
| - Allows administrators to send commands to screens.
|
*/

export default function ScreenCommands() {
    /*
    |--------------------------------------------------------------------------
    | Render Component
    |--------------------------------------------------------------------------
    |
    | Renders the screen command panel.
    |
    */
    return <ScreenCommandPanel />
}