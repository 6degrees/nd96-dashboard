services:

  # =====================================================
  # Next.js Branch Dashboard
  # =====================================================

  frontend:
    build: context: .
    dockerfile: Dockerfile

    restart: unless-stopped

    environment:
      NODE_ENV: production
      PORT: 3000
      HOSTNAME: 0.0.0.0

    ports:
      - "3001:3000"